Offer routes are project-local and adapted from `@ensotek/shared-backend/modules/offer`.

This project keeps its schema in `src/db/seed/sql/013_offers.sql` so fresh
seeds create the offer tables without `ALTER TABLE`.
